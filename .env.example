# Copy to .env and fill in. .env is gitignored.

# Notion integration token. Create at https://www.notion.so/my-integrations
# Then in Notion, open the Settlements parent page > ... > Add connections > Settlements Sync.
NOTION_API_KEY=
