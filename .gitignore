# Secrets
.env
.env.*
!.env.example

# Notion sync state (per-row hashes — local cache, not source of truth)
.notion-sync-state.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Build artefacts
*.stl.bak
*.blend1

# Large binaries (track via DVC / git-lfs if needed later)
# *.stl
# *.blend
# *.zip

# Logs
*.log

# TTS: the generated save is a build artifact — regenerate with tts/build_table.py
tts/Settlements.json
tts/pulled/
tts/assets/models/*_assembly.stl
