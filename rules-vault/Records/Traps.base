views:
  - type: table
    name: All
    rowHeight: medium
    filters:
      and:
        - type == "traps"
    order:
      - file.name
      - build
      - credits
      - trigger_effect
  - type: table
    name: By build
    rowHeight: medium
    filters:
      and:
        - type == "traps"
    order:
      - build
      - file.name
      - credits
      - trigger_effect
    sort:
      - property: build
        direction: ASC
